// Define an array of numbers to be used
const numbers = [1, 4, 9];

// .map() method to create a new array with square roots
const squareRoots = numbers.map((number) => Math.sqrt(number));

// Print the new array with square roots
console.log(squareRoots);
